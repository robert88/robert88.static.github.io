Object.keys
Object.getOwnPropertyDescriptor
Object.defineProperty
------------------------------
Object.defineProperty
http://imweb.io/topic/56d40adc0848801a4ba198ce
1、Object.defineProperty(object, propertyname, descriptor)
descriptor
【value】 属性的值，默认为 undefined。
【writable】这个属性该特性默认值为为 true。
【configurable]】这个属性该特性默认值为为 true。 
【enumerable】这个属性该特性默认值为为 true。 
【get】一旦目标对象访问该属性，就会调用这个方法，并返回结果。默认为 undefined。
【set】 一旦目标对象设置该属性，就会调用这个方法。默认为 undefined。

1、MVVM中数据‘双向绑定’实现
2、优化对象获取和修改属性方式；
3、增加属性获取和修改时的信息
