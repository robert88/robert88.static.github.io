loader-utils 

一、參數query
1、?url=t&arr[]=1&-no=&+yes=&go;可以解析成{url:t,arr=[1],no:false,yes=true,go=true}
2、"?{url:t,arr=[1]}"可以解析成{url:t,arr=[1]}
3、{url:t,arr=[1]}直接返回{url:t,arr=[1]}

二、參數options:{name:""}
name || "[hash].[ext]";
name也可以是function最后也是要返回一個字符串
替換名稱有
emoji
hash
ext
name
path
folder

html-minifier


schema-utils

提供一個固定的數據對象
{ type: 'object',
  properties:
   { name: {},
     regExp: {},
     context: { type: 'string' },
     publicPath: {},
     outputPath: {},
     useRelativePath: { type: 'boolean' },
     emitFile: { type: 'boolean' } },
  errorMessages:
   { context: 'should be {String} (https://github.com/webpack-contrib/file-loade
     useRelativePath: 'should be {Boolean} (https://github.com/webpack-contrib/f
     emitFile: 'should be {Boolean} (https://github.com/webpack-contrib/file-loa
  additionalProperties: true }
  }
  
  ajv

  ajv-keywords

  emojis-list